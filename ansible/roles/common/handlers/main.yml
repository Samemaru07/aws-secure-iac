---
- name: "Restart ssh" # タスクのnotifyと完全一致
  ansible.builtin.systemd:
      name: "ssh"
      state: "restarted"

- name: "Reload Nginx"
  ansible.builtin.systemd:
      name: "nginx"
      state: "reloaded"
